{
  "name": "stripe-azure-function",
  "version": "1.0.0",
  "description": "Stripe PaymentIntent Azure Function",
  "main": "index.js",
  "scripts": {
    "start": "func start",
    "test": "echo \"Skipping tests\" && exit 0"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  }
}
